<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arena</title>
</head>
<body>
    <h1>Battlegrounds</h1>
    

    <div><%= JSON.stringify(pokemon).replace(/\"/g, "") %></div>

    <% for (let i = 0; i < attack.length; i++){%>
        <button> <%= JSON.stringify(attack[i]).replace(/\"/g, "") %>    </button>
   <%} %>

   <div><%= JSON.stringify(opponents).replace(/\"/g, "") %></div>
</body>
</html>